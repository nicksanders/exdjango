defmodule Zlib do

  def decompress(data, wbits \\ 15) do
    # z = :zlib:open()
    # :zlib.inflateInit(z, wbits)
    # result = :zlib.inflate(z, data)
    # :zlib.inflateEnd(z)
    # result
    1
  end

end
